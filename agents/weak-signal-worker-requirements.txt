# Weak Signal Worker Dependencies
# For integration of WeakSignalFinder output into PCA Capture pipeline

# Core
python-dateutil>=2.8.2

# Optional: For persistence and state management
sqlalchemy>=2.0.0
pydantic>=2.0.0

# Optional: For metrics and monitoring
prometheus-client>=0.17.0

# Optional: For async processing (if integrated with Celery/n8n)
aiofiles>=23.1.0
